module SurvivalsHelper
  # def median(array)
  #   if array.present?
  #     sorted = array.sort
  #     len = sorted.length
  #     (sorted[(len - 1) / 2] + sorted[len / 2]) / 2.0
  #   else
  #     return
  #   end
  # end
  #
  # def calculate_percentile(array, percentile)
  #   array.sort[(percentile * array.length).ceil - 1]
  # end
end
